{
  "name": "shein-price-tracker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute shein-tracker-db --local --file schema.sql",
    "db:remote": "wrangler d1 execute shein-tracker-db --remote --file schema.sql",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
