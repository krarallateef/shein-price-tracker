# انسخه إلى fetcher/.env على جهاز الأندرويد (مُتجاهَل من git).

# رابط Worker بعد النشر (بلا / في النهاية)
WORKER_URL=https://shein-price-tracker.YOUR-SUBDOMAIN.workers.dev

# نفس القيمة التي وضعتها بـ: wrangler secret put INGEST_TOKEN
INGEST_TOKEN=
